# Lingua · 玻璃划词卡片 v2.3.0

## 六套卡片主题

在右上角「自定义卡片」最上方选择主题，点击后立即生效并保存。默认使用 **iOS 玻璃**。

| 主题 | 界面设计 |
| --- | --- |
| iOS 玻璃 | 清透冷白、蓝色强调、无装饰横幅，保持紧凑 |
| 咱们裸熊 | 奶油纸色、焦糖按钮、棕熊 / 熊猫 / 白熊装饰 |
| Hello Kitty 和伙伴 | 樱花粉、礼物般的白色面板、Kitty / My Melody / Kuromi 装饰 |
| Google · Material | 轻蓝底色、四色几何、圆润例句面板 |
| 卡通涂鸦 | 浅黄便签、手绘字感、紫色控件、星星与笑脸、描边例句框 |
| Chiikawa | 奶油粉与淡蓝、柔和控件、Chiikawa / Hachiware / Usagi 装饰 |

所有主题统一调整正文、辅助文字、按钮、开关、滑块、输入框、例句框和 SVG 装饰。角色与品牌风格主题为本项目绘制的非官方主题。选择主题不会修改翻译服务、Key 或展示模块；原应用 Logo 保持不变。

主题资源位于 `Themes.cs` 和 `themes.svg`，无需下载远程图片。使用 `--theme-preview` 可以渲染六套主题总览。

已接入正式 Logo，应用 EXE、窗口标题和系统托盘使用统一图标。Logo 原图保留透明背景；Windows 图标包含 16–256 像素多个尺寸。

本次更新：关闭系统边框描边，统一原生窗口与卡片圆角，去除边缘重复轮廓。已用原生窗口截图核对，示例截图为 `native-edge-preview.png`。

## 只填 Key 的模型预设

打开右上角设置，在顶部选择服务、模型分类，填写 Key，点击「保存并使用」。地址与请求参数自动配置，不必手写 URL 或模型名称。可以先点击「测试连接」，它会使用固定文本 `hello` 发起一次真实调用，不自动保存配置。

| 服务 | 快速翻译 | 均衡学习 | 深入解析 |
| --- | --- | --- | --- |
| OpenAI | gpt-4.1-mini | gpt-4.1 | gpt-5-mini（低推理强度） |
| DeepSeek | deepseek-v4-flash（不思考） | deepseek-v4-pro（不思考） | deepseek-v4-pro（思考） |
| Qwen | qwen-flash | qwen-plus | qwen3-max |

分类是本工具按学习场景设计的选项，并非服务商官方套餐名称；不是完整模型目录，也不保证每个账户均有调用权限。预设按 2026-09-15 获取的官方文档核对：[OpenAI 模型](https://developers.openai.com/api/docs/models/all)、[DeepSeek 模型与接口](https://api-docs.deepseek.com/)、[Qwen 兼容接口](https://help.aliyun.com/zh/model-studio/qwen-api-via-openai-chat-completions)。

Qwen 分北京和新加坡，请选择 Key 所属地域。使用官方仍支持的通用域名，不要求填写业务空间 ID。Qwen 预设关闭思考，便于快速返回结构化学习内容。

各服务和地域的 Key 分别加密保存。切换服务不会带入其他服务的 Key；未保存的输入只在当前窗口会话内保留。旧版自定义配置会保留，自定义接口入口仍可用。Key 只在本地应用内填写，无需发到聊天中。

双击 **LinguaSelect.exe**。这版把原来的大窗口改为紧凑的浮动玻璃卡片，不再需要点击「译」或切换标签页。

## 交互

- 在支持读取选区的软件中选中文字，松开鼠标后自动查询并弹出卡片，不抢走原软件的焦点。基础译文先显示，词典随后补全。
- 右上角从左到右：输入文本、固定卡片、自定义卡片、收起卡片。
- 点击卡片之外会收起；固定后保留。拖动顶部品牌区域可以移动卡片。
- 原文旁的喇叭：朗读 / 停止；复制图标：复制译文。
- 双击原文或点击铅笔可手动输入。输入框支持 Ctrl+Enter 查询。
- 右键可重新查询、输入文本、复制原文或退出。Esc 收起卡片。系统托盘也可以打开卡片、进入设置、暂停划词和退出。
- Ctrl+Alt+T 手动读取当前选区。如果软件不公开选区，可自行复制后在输入框粘贴。

## 自定义

点击右上角的滑杆图标：

- 分别开关原文与朗读、音标、译文、多种释义、双语例句、句式与搭配。
- 开关紧凑排版；内容隐藏后卡片会随之缩短。
- 拖动「玻璃浓度」调整背景透明程度，文字本身保持清晰。
- 选择目标语言、朗读声音，或暂停自动翻译。

展示项、紧凑排版、玻璃浓度、目标语言和自动翻译开关保存到本地，重启后保留。语音选择在本次运行中有效。

## 玻璃效果与 SVG

采用 WPF 矢量界面、圆角、半透明渐变和 Windows Desktop Acrylic 背景。Windows 11 22H2 及以后使用系统背景模糊；Windows 10 使用半透明卡片回退，系统关闭透明效果或限制合成时实际外观可能不同。

`icons.svg` 是原创图标源，已嵌入 EXE 并在运行时以矢量路径绘制。无需字体图标、图片文件或浏览器运行时。

`界面预览.png` 使用真实界面控件渲染，搭配示例文本和设计背景；不是实时翻译结果，也不是桌面模糊效果的截图。

## 翻译能力

基础模式无需密钥：MyMemory 提供主要译文，Free Dictionary API 提供英语单词释义、音标及可用例句。基础词典没有例句时明确提示；基础模式不生成句式解析。默认识别英语 / 中文，可切换七种目标语言。

完整的多种表达、双语例句和句式解析需要你自己的 AI 服务。使用上面的预设即可；使用其他服务时选择「自定义兼容接口」。没有使用用户凭据实测模型返回效果。

基础翻译每次限制 500 UTF-8 字节；AI 模式每次最多 2000 字符。公共服务可能限流或不可达。朗读依赖 Windows 已安装的语音包。

## 数据说明

自动划词会直接将选中文本发送到服务：基础模式使用 MyMemory，英语词条另查询 Free Dictionary API；AI 模式发送到配置的服务。不会读取全文或自动改写剪贴板。

本次运行缓存最多 20 个结果，加速重复查询；退出后清除，不写入磁盘。设置位于 `%LOCALAPPDATA%\LinguaSelect\settings.json`，与上一版共用。密钥使用 Windows DPAPI 为当前用户加密保存。原先需要鼠标停留的触发模式已迁移为直接显示。

词典来源与许可放在底部状态文字的悬浮提示中，可将鼠标停在状态文字上查看。

## 兼容性

划词仍依赖 Windows UI Automation TextPattern。浏览器、Word、PDF、自绘编辑器的兼容性需要逐项试用；不支持图片 OCR、扫描 PDF、管理员窗口和安全桌面读取。不支持的场景可以手动粘贴。可在设置中排除指定进程。

## 构建与测试

需要 .NET Framework 4.8。源码、XAML、SVG、清单和构建脚本均已包含。

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\build.ps1
.\LinguaSelect.exe --self-test --network
.\LinguaSelect.exe --ui-test
.\LinguaSelect.exe --preview
```

界面测试检查了不抢焦点、直接查询模式迁移、六项开关、卡片高度收缩、设置切换、透明程度、SVG 加载、系统边框关闭、圆角一致及只填 Key 的配置流程。离线测试验证各模型预设参数、Key 隔离与加密、旧配置迁移。编译和测试记录随包提供。没有真实 AI Key，因此未声称已验证三个服务商的在线鉴权、额度或实际模型结果；也未声称所有外部应用均兼容。

技术参考：[Desktop Acrylic](https://learn.microsoft.com/en-us/windows/win32/api/dwmapi/ne-dwmapi-dwm_systembackdrop_type)、[MyMemory](https://mymemory.translated.net/doc/spec.php)、[Free Dictionary API](https://dictionaryapi.dev/)。
